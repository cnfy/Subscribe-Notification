services:
  - type: web
    name: Subscribe Notification
    env: python
    plan: free
    buildCommand: |
      pip install -r requirements.txt
      playwright install
    startCommand: gunicorn app:app